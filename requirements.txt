# Pinned to the known-good versions that produced the diploma results
# (the Colab A100 run). PEP 440 == specifiers ignore local build labels, so
# torch==2.10.0 is satisfied by a preinstalled 2.10.0+cu128 (Colab/Kaggle GPU)
# and installs the CPU wheel locally. Reproducible on local / Colab / Kaggle.
nilearn==0.13.1
nibabel==5.4.2
numpy==2.0.2
pandas==2.2.2
matplotlib==3.10.0
seaborn==0.13.2
scikit-learn==1.6.1
plotly==6.7.0
umap-learn==0.5.12
torch==2.10.0
kaleido==1.3.0
shap==0.51.0
neuroCombat==0.2.12
scipy==1.16.3
pytest==8.4.2
tqdm==4.67.3
