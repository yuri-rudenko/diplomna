# Pinned to the known-good versions that produced the diploma results
# (the Colab A100 run). Reproducible on local / Colab / Kaggle.
# torch is a FLOOR, not an exact pin: Colab/Kaggle preinstall a CUDA-matched
# build, and an exact pin would force a multi-GB downgrade/re-download and clash
# with the platform's torchvision. Any torch 2.x runs this code unchanged.
nilearn==0.13.1
nibabel==5.4.2
numpy==2.0.2
pandas==2.2.2
matplotlib==3.10.0
seaborn==0.13.2
scikit-learn==1.6.1
plotly==6.7.0
umap-learn==0.5.12
torch>=2.0
kaleido==1.3.0
shap==0.51.0
neuroCombat==0.2.12
scipy==1.16.3
pytest==8.4.2
tqdm==4.67.3
